# Python バックエンド（FastAPI）の URL
# ローカル開発時は backend で docker compose up 後のデフォルト
NEXT_PUBLIC_API_URL=http://localhost:8000

# 管理画面ログイン用パスワード（/admin）
ADMIN_PASSWORD=

# バックエンド管理 API 呼び出し用キー（サーバー側のみ。Render の ADMIN_API_KEY と同じ値）
ADMIN_API_KEY=
